RSpec.describe Es::Rails::Searchable do
  it 'has a version number' do
    expect(Es::Rails::Searchable::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
